[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
domain = { path = "../domain" }
shared = { path = "../shared" }
application = { path = "../application" }

rocket = { version = "0.5.0-rc.2", features = ["json"] }
serde_json = "1.0.88"